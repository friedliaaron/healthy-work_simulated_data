# Healthy@Work Analytics Dashboard

## Setup (einmalig)

```r
install.packages("renv")
renv::restore()
```

## App starten

```r
shiny::runApp()
```

Oder in RStudio: `app.R` öffnen → **Run App** klicken.
